# Daily data files

Create one file per betting day when useful, for example `2026-08-13.json`.

Use the same shape as `data/current_schedule.json`. Before asking Codex for a command-line betting recommendation, either update `data/current_schedule.json` directly or copy the latest daily file content into it.
